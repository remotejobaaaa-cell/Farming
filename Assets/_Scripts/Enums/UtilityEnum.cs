public enum ControlsType
{
    Buttons,
    Steering,
    Tilt
}

public enum Quality
{
    Low,
    Medium,
    High
}

public enum GameplayWeather
{
    SunShine,
    Eveining,
    Night
}

public enum CameraFocus
{
    StartMenu,
    FarmerSelection
}

public enum CurrentScene
{
    GarageScene,
    GameplayScene
}

public enum LevelType
{
    ThirdPerson,
    Vehicle
}

public enum VehicleType
{
    Tractor,
    Harvestor,
    PickupTruck,
    Loader,
    Drone
}

public enum TimeMode
{
    UseTime,
    Unlimited
}

public enum Cinematics
{
    No,
    Yes
}